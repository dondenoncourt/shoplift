	<div class="face-circle"><img src="/img/face-circle.png" /></div>
	<div class="circle-overflow">
		{{#if thumbUrl}}
	  	<img {{bindAttr src="thumbUrl"}} {{action showProfile this}} width="116" height="87" />
	  {{else}}
	  	<img src="/assets/avatars/small/missing.png" {{action showProfile this}} width="116" height="87" />
	  {{/if}}
	</div>
	<h2 {{action showProfile this}}>{{truncate fullName length=15 append="..."}}</h2>
